info:
  name: scan
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:3000/scan
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "url": "https://github.com/SurajG20/BudgetWise"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 600000
  followRedirects: true
  maxRedirects: 5
